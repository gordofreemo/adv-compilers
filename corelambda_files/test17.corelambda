
app(
    abs(
        x:Variant(number:Int, boolean:Bool, u:Unit)
        .
        case 
            x 
        of 
            number=n => =(0,n) 
        | 
            boolean=b => if b then false else true fi 
        |
            u=u => true
        esac
        ),
    tag(
        number=1 as Variant(number:Int, boolean:Bool)
    )
)
